from setuptools import setup, find_packages

setup(
    name="gearpy",
    version="0.1.0",
    author="Your Name",
    description="Auto-compiles and parallelizes CPU-bound Python code using a simple decorator.",
    packages=find_packages(),
    install_requires=[],
    python_requires=">=3.7",
)
